server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/Projects/edutech/edutech/config-server/src/main/resources/shared-configs/ # path to files storage

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

