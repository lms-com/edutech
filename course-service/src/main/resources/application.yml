spring:
  application:
    name: course-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 5
  config:
    import: "optional:configserver:http://localhost:8888"
